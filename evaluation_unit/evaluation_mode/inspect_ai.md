## Evaluation Mode Categories

[Static Analysis, Dynamic Execution]

## Detailed Analysis

### Static Analysis

Evidence 1: Pattern matching for answer extraction
- File: `examples/scorer.py`
- Function: `expression_equivalence()`
- Code Reference:
```python
match = re.search(AnswerPattern.LINE, state.output.completion)
if match:
    answer = match.group(1)
```
This scorer uses regex pattern matching (`re.search()`) to extract answers from model outputs and performs text comparison without executing model-generated code. The evaluation relies purely on analyzing the textual structure of completions.

Evidence 2: Token and timing validation scorers
- File: `tests/test_sample_limits.py`
- Functions: `token_consuming_scorer()`, `slow_scorer()`
- Code Reference:
```
token_consuming_scorer() - Validates token usage through inspection
slow_scorer() - Evaluates timing properties
```
These scorers analyze model outputs through comparison and validation without execution. They assess intrinsic properties like token consumption and timing characteristics through static inspection.

Evidence 3: Built-in text comparison scorers
- Files: `tests/test_sample_shuffle.py`, `tests/test_extensions.py`, `tests/test_retry.py`
- Functions: `inspect_ai.scorer.match()`, `inspect_ai.scorer.includes()`, `inspect_ai.scorer.exact()`
- Code Reference:
```
match() - String/pattern matching
includes() - Substring/inclusion checking
exact() - Exact string comparison
```
These perform text-based validation without executing generated artifacts. The harness uses pattern matching, string comparison, and format validation to evaluate model outputs purely through textual analysis.

Evidence 4: Media content response validation
- Files: `tests/util/test_document.py`, `tests/util/test_images.py`, `tests/util/test_media.py`
- Scorer: `includes()`
- Code Reference:
```
includes() scorer validates model responses about media content
```
The evaluation analyzes text outputs describing images, documents, and videos without executing any code. This demonstrates static analysis of model-generated descriptions and interpretations of media content.

---

### Dynamic Execution

Evidence 1: Sandboxed Python script execution
- File: `tests/util/sandbox/test_sandbox_service.py`
- Code Reference:
```python
result = await sandbox().exec(["python3", run_script], user=user)
```
The harness executes Python scripts in sandboxed environments, providing runtime validation of model-generated code. This infrastructure supports controlled execution with isolation and security constraints.

Evidence 2: Docker container execution
- Files: `tests/util/sandbox/test_docker_compose_multiple_services.py`, `tests/util/sandbox/test_multi_sandbox_context_management.py`
- Code Reference:
```
Runs Docker containers and executes commands
Manages multiple execution environments
```
The framework runs Docker containers and manages multiple execution contexts simultaneously. This demonstrates comprehensive support for containerized execution environments with process isolation.

Evidence 3: Tool-based command execution
- File: `tests/test_extensions.py`
- Tool: `bash()`
- Code Reference:
```python
task = Task(
    dataset=[Sample(input="Please use the list_files tool...")],
    solver=[use_tools(list_files()), generate()],
)
```
The bash tool executes shell commands generated by models, along with file system operations (write_file, read_file, exec) in sandboxes. This enables functional testing of model-generated system commands.

Evidence 4: Direct subprocess execution with controls
- File: `tests/util/test_subprocess.py`
- Code Reference:
```python
result = await subprocess(["python3", "-c", "print('foo')"])
```
The harness executes commands with timeout, environment variables, and working directory control. It tests process termination and signal handling, providing fine-grained control over code execution.

Evidence 5: Execution limits and resource constraints
- Files: `tests/util/test_limit_working.py`, `tests/test_sample_limits.py`
- Code Reference:
```python
ModelOutput.for_tool_call(
    model="mockllm/model",
    tool_name="bash",
    tool_arguments={"cmd": "sleep 100"},
)
```
The framework tests token limits during model-generated code execution and verifies execution timeouts and resource constraints. This demonstrates runtime validation with safety guardrails for controlling execution behavior.