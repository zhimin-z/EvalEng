## Evaluation Mode Categories

[Static Analysis]

## Detailed Analysis

### Static Analysis

Evidence 1: Inference fetching without automated evaluation
- File: `src/components/queries/index.ts`
- Function: `get_inference()`
- Code Reference:
```typescript
get_inference()
```
The function calls a Tauri backend that communicates with an Ollama server to generate text completions. However, the generated outputs are only fetched and displayed, not programmatically evaluated. The tool presents outputs side-by-side for human comparison without any automated scoring functions, similarity calculations, format validators, or pattern matching evaluators.

Evidence 2: Manual inspection through visual interface
- File: `README.md`
- Description: User interface for manual comparison
- Key Features:
  - "allowing you to iterate over their combinations and to visually inspect the results"
  - "Allows user to hide model names on the results pane, to avoid bias in evaluations"

Users visually compare text outputs generated by different models and parameter combinations through a display-focused interface. The inspection is entirely manual and subjective, with no automated metrics or evaluation logic.

Evidence 3: Metadata display without evaluation logic
- File: `src/lib/index.ts`
- Functions: `formatInterval()`, `tokensPerSecond()`, `convertEpochToDateTime()`
- Code Reference:
```typescript
formatInterval()
tokensPerSecond()
convertEpochToDateTime()
```
The application provides helper functions for presenting basic metadata such as tokens per second and duration formatting. These are display utilities rather than evaluation metrics, focusing on presenting results for human review rather than computing quality scores.

Evidence 4: Experiment logging for review
- File: `src/issue_with_sqlx.md`
- Description: SQLite database schema
Results are saved to a SQLite database for later review, but this storage serves as a log of experiments rather than an automated evaluation pipeline. No automated evaluation occurs on the stored data; it exists purely for human reference and manual comparison.