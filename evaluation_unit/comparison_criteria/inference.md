## Comparison Criteria Categories

[Explicit Labels, Behavioral Specification, None]

## Detailed Analysis

### Explicit Labels

Evidence 1: Speech Recognition Reference Transcripts
- File: `speech2text/accuracy_eval.py`
- Code Reference: Transcript loading and WER computation (Lines 59-60, 67-68)
```
references.append(manifest[result["qsl_idx"]]["transcript"])
references = __gather_predictions([references], labels=labels)
hypotheses = __gather_predictions([hypotheses], labels=labels)
wer, _, _ = word_error_rate(hypotheses=hypotheses, references=references)
```
The harness loads ground truth transcripts from the manifest dataset for each query sample index. These transcripts serve as explicit labels to evaluate model-generated speech transcriptions by computing Word Error Rate (WER).

Evidence 2: Retired Speech Recognition Transcripts
- File: `retired_benchmarks/speech_recognition/rnnt/accuracy_eval.py`
- Code Reference: Reference transcript loading (Lines 48-51)
```
hypotheses.append(array.array(dtype_map[args.output_dtype], bytes.fromhex(result["data"])).tolist())
references.append(manifest[result["qsl_idx"]]["transcript"])
references = __gather_predictions([references], labels=labels)
hypotheses = __gather_predictions([hypotheses], labels=labels)
```
Similar to the active speech benchmark, this loads transcript labels from the manifest for evaluation against model predictions, providing explicit ground truth for accuracy assessment.

Evidence 3: Translation Reference Files
- File: `retired_benchmarks/translation/gnmt/tensorflow/nmt/inference_test.py`
- Code Reference: Reference file specification (Lines 72-73)
```
FLAGS.inference_ref_file = "nmt/testdata/" "iwslt15.tst2013.100.vi"
```
- File: `retired_benchmarks/translation/gnmt/tensorflow/nmt/utils/evaluation_utils.py`
- Code Reference: Reference text loading (Lines 46-50)
```
ref_files = [ref_file]
reference_text = []
for reference_filename in ref_files:
    with codecs.getreader("utf-8")(tf.gfile.GFile(reference_filename, "rb")) as fh:
        reference_text.append(fh.readlines())
```
The harness loads reference translations from files specified in the configuration. These serve as gold standard translations for computing BLEU scores against model-generated translations.

Evidence 4: COCO Object Detection Annotations
- File: `vision/classification_and_detection/tools/coco-analyze.py`
- Code Reference: Annotation loading (Lines 142-145)
```
annotation_file = os.path.join(args.coco, "annotations/instances_val2017.json")
cocoGt = COCO(annotation_file)
annotate_image(results, cocoGt, args.output)
calculate_map(args.input, cocoGt, args.output)
```
The COCO annotations file contains ground truth bounding boxes and category labels for object detection. These explicit labels are used to compute mAP (mean Average Precision) metrics for evaluating detection quality.

Evidence 5: Text-to-Image Reference Statistics
- File: `text_to_image/tools/fid/fid_score.py`
- Code Reference: Reference statistics loading (Lines 194-196)
```
def compute_fid(results, statistics_path, device, ...):
    # ...
    m1, s1 = compute_statistics_of_path(statistics_path, model, batch_size, dims, device, num_workers, subset_size, shuffle_seed)
```
The FID (Fréchet Inception Distance) computation loads pre-computed statistics (mean and covariance) from reference images stored in `statistics_path`. These serve as explicit statistical labels representing the distribution of real images for comparison.

---

### Behavioral Specification

Evidence 1: Graph Benchmark Validation
- File: `graph/R-GAT/tools/download_igbh_test.py`
- Code Reference: Dataset download (Lines 30-37)
```
download.download_dataset(
    path=args.target_path,
    dataset_size=args.dataset_size,
    dataset_type=args.dataset_type,
)
```
- File: `graph/R-GAT/benchmark-checklist.md`
- Code Reference: Validation checklist (Lines 44-46, 57-59)
```md
- [X] `accuracy.txt` file generated by the reference accuracy script from the MLPerf accuracy log and is validated by the submission checker.
- [X] Reference implementation successfully processes the entire validation dataset during:
  - [X] Performance runs
  - [X] Accuracy runs
  - [X] Compliance runs
```
The R-GAT benchmark includes functional validation through dataset processing and submission checker validation. The checklist indicates the harness validates model outputs through compliance checks and submission validation, providing executable specifications that verify correctness of benchmark execution.

---

### None

Evidence 1: Text Normalization Processing
- File: `speech2text/accuracy_eval.py`
- Code Reference: `word_error_rate()` function (Lines 26-44)
```
from whisper.normalizers import EnglishTextNormalizer

def word_error_rate(hypotheses: List[str], references: List[str]) -> float:
    normalizer = EnglishTextNormalizer()
    scores = 0
    words = 0
    for h, r in zip(hypotheses, references):
        h = normalizer(h)
        r = normalizer(r)
        h_list = h.split()
        r_list = r.split()
        scores_clip, words_clip = compute_wer_with_concatenation(h_list, r_list)
        scores += scores_clip
        words += words_clip
    wer = scores / words
    return wer, scores, words
```
While this computes WER using references, the `normalizer` applies intrinsic text normalization to both hypotheses and references. The normalization process itself is a reference-free quality measure that standardizes text format independent of ground truth comparison.

Evidence 2: Levenshtein Distance Calculation
- File: `retired_benchmarks/speech_recognition/rnnt/pytorch/metrics.py`
- Code Reference: `__levenshtein()` function (Lines 10-28)
```
def __levenshtein(a: List, b: List) -> int:
    """Calculates the Levenshtein distance between a and b."""
    n, m = len(a), len(b)
    if n > m:
        a, b = b, a
        n, m = m, n
    current = list(range(n + 1))
    for i in range(1, m + 1):
        previous, current = current, [i] + [0] * n
        for j in range(1, n + 1):
            add, delete = previous[j] + 1, current[j - 1] + 1
            change = previous[j - 1]
            if a[j - 1] != b[i - 1]:
                change = change + 1
            current[j] = min(add, delete, change)
    return current[n]
```
The Levenshtein distance calculation is an intrinsic metric measuring edit distance between sequences. While used in conjunction with references, the distance computation itself is a self-contained algorithmic assessment of sequence similarity.

Evidence 3: Inception Feature Extraction
- File: `text_to_image/tools/fid/fid_score.py`
- Code Reference: `get_activations()` function (Lines 71-106)
```
def get_activations(files, model, batch_size=50, dims=2048, device="cpu", num_workers=1):
    """Calculates the activations of the pool_3 layer for all images."""
    model.eval()
    # ...
    for batch in dataloader:
        batch = batch.to(device)
        with torch.no_grad():
            pred = model(batch)[0]
        if pred.size(2) != 1 or pred.size(3) != 1:
            pred = adaptive_avg_pool2d(pred, output_size=(1, 1))
        pred = pred.squeeze(3).squeeze(2).cpu().numpy()
        pred_arr[start_idx: start_idx + pred.shape[0]] = pred
        start_idx = start_idx + pred.shape[0]
    return pred_arr
```
Extracts intrinsic feature representations from images using an Inception network. The feature extraction process is a reference-free quality measure that characterizes images based on learned representations, independent of any ground truth labels.